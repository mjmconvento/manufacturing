{% extends "CatalystTemplateBundle::base.html.twig" %}
{% import "CatalystTemplateBundle::form.html.twig" as form_tools %}

{% block content %}
    <div class="row">
    <div class="col-md-12">
        <div class="portlet box blue-hoki">
            <div class="portlet-title">
                <div class="caption">Request Lists{#{{ list_title }}#}</div>
                <div class="actions">
                </div>
            </div>
            <div class="portlet-body">
                <div class="table-toolbar">
                {#{% if app.user.hasAccess(prefix ~ '.add') %}
                    <a href="{{ path(route_add) }}" class="btn green">Add New</a>
                {% endif %}#}
                <div class="form-group">
                    <div class="col-md-10">
                        {#<a id="btn_en_add" href="#en_form" class="btn green" data-toggle="modal">Export to CSV</a>
                        <a id="btn_en_add" href="#en_form" class="btn green" data-toggle="modal">Print</a>#}
                    </div>
                </div>
                <div class="dataTables_filter" id="list_table_filter">
                    <label>Search: 
                        <input type="text" aria-controls="list_table" class="form-control input-small input-inline">
                    </label>
                </div>            
                </div>
                <table class="table table-striped table-bordered table-hover" id="list_table">
                    <thead>
                        <tr>
                            <th>P.O. Receipt #</th>
                            <th>Date Created</th>
                            <th>Target Delivery Date</th>
                            <th>Branch</th>
                            <th>SKU</th>
                            <th>Qty</th>
                            <th>Amt. Value</th>
                            <th>Status</th>                            
                            <th style="width: 50px"></th>                     
                        </tr>
                    </thead>
                    
                    <tbody>
                        <tr>
                            <td>PO-2014-0001</td>                            
                            <td>11/26/2014</td>                                                       
                            <td>12/01/2014</td>
                            <td>Makati-Rada</td>
                            <td>3</td>
                            <td>4</td>
                            <td>120</td>
                            <td></td>
                            <td><a href="{{ path('serenitea_rlist_edit')}}{##btn_en_add#}" class="btn default btn-xs blue-madison">View</a></td>  
                        </tr>
{#                    {% endfor %}#}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}
