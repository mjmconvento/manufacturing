{% extends "CatalystTemplateBundle::base.html.twig" %}
{% import "CatalystTemplateBundle::form.html.twig" as form %}

{% block content %}
<div class="row">
    <div class="col-md-12">
        <div class="portlet box blue-hoki">
            <div class="portlet-title">
                <div class="caption">{#{{ list_title }}#}Deliveries List</div>
                <div class="actions">
                </div>
            </div>
            <div class="portlet-body">
                <div class="table-toolbar" style="height:40px">
                    
                    <div class="form-group">
                    <div class="col-md-3">
                        {#<a id="btn_en_add" href="#en_form" class="btn green" data-toggle="modal">Export to CSV</a>
                        <a id="btn_en_add" href="#en_form" class="btn green" data-toggle="modal">Print</a>#}
                    </div>
                </div>
                    
                    <div class="col-md-4">                        
                        <div class="input-group" id="daterange">
                            <input type="text" class="form-control" placeholder="Date Range" value="11/22/2014   -   11/28/2014" readonly="">
                            <span class="input-group-btn">
                            <button class="btn default date-range-toggle" type="button"><i class="fa fa-calendar"></i></button>
                            </span>
                        </div>
                    </div>
                                                          
                </div>
                <div class="table-toolbar" style="height:40px">
                    <div class="form-group">
                    <div class="col-md-10">
                        {#<a id="btn_en_add" href="#en_form" class="btn green" data-toggle="modal">Export to CSV</a>
                        <a id="btn_en_add" href="#en_form" class="btn green" data-toggle="modal">Print</a>#}
                    </div>
                </div>
                <div class="dataTables_filter" id="list_table_filter">
                    <label>Search: 
                        <input type="text" aria-controls="list_table" class="form-control input-small input-inline">
                    </label>
                </div>            
                </div>
                {#<div class="table-toolbar">
                {% if app.user.hasAccess(prefix ~ '.add') %}
                    <a href="{{ path(route_add) }}" class="btn green">Add New</a>
                {% endif %}
                </div>#}
                <table class="table table-striped table-bordered table-hover" id="list_table">
                    <thead>
                        <tr>                            
                            <th style="width:150px">DR Receipt #</th>
                            <th>Date Created</th>
                            <th>Delivery Date</th>                            
                            <th>Branch</th>
                            <th>Items</th>
                            <th>Quantity</th>
                            <th>Amount</th>
                            <th>Status</th>
                            <th></th>
                        {#{% for col in grid_cols %}
                            {% if (loop.last) %}
                            <th style="width: 90px">{{ col.getLabel }}</th>
                            {% else %}
                            <th>{{ col.getLabel }}</th>
                            {% endif %}
                        {% endfor %}#}
                        </tr>
                    </thead>
                    <tbody>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>                        
                        <td></td>
                        <td></td>
                        <td style="width: 100px"><a href="{{ path('serenitea_dlist_view') }}" class="btn default btn-xs blue-madison">View</a></td>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}